<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Rest timer notifications. The user works out with the screen off, so
         the alert must fire even in Doze. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <uses-permission android:name="android.permission.VIBRATE"/>
    <!-- The rest alert is an alarm: it is worthless a minute late. On API 33+
         USE_EXACT_ALARM grants that at install time, so the athlete is never
         thrown into "알람 및 리마인더" settings mid-workout. Below 33 the
         SCHEDULE_EXACT_ALARM flavour is granted by default anyway.
         (Play Store distribution would need a declaration for USE_EXACT_ALARM;
         this app is sideloaded — see docs/06-ANDROID-SIGNING.md.) -->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM"
                     android:maxSdkVersion="32"/>
    <uses-permission android:name="android.permission.USE_EXACT_ALARM"/>

    <application
        android:label="Workout Log"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>

            <!-- Routine exchange files (docs/04-ROUTINE-EXCHANGE.md).

                 Only application/json is claimed, deliberately. Filtering on
                 */* would put Workout Log in the share sheet of every photo
                 and link on the phone; a .json that some app labels as
                 text/plain can still be imported through 루틴 목록 →
                 가져오기. -->
            <intent-filter>
                <action android:name="android.intent.action.SEND"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <data android:mimeType="application/json"/>
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.BROWSABLE"/>
                <data android:scheme="content" android:mimeType="application/json"/>
                <data android:scheme="file" android:mimeType="application/json"/>
            </intent-filter>
        </activity>
        <!-- Posts the scheduled rest alert when AlarmManager fires. Since
             flutter_local_notifications 16 the plugin no longer declares this
             in its own manifest, and without it the alarm goes off into
             nothing: the countdown ends and no notification ever appears.

             Its sibling ScheduledNotificationBootReceiver is deliberately left
             out. It re-arms pending alarms after a reboot, which for a rest
             timer means buzzing "휴식 완료" on the boot that interrupted it. A
             rest does not survive a reboot; RECEIVE_BOOT_COMPLETED is gone with
             it. -->
        <receiver
            android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationReceiver"
            android:exported="false"/>

        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
