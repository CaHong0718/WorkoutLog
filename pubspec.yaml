name: health_app
description: "무분할 40분 루틴 기반 운동 기록 앱"
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: ^3.12.2

dependencies:
  drift: ^2.34.3
  drift_flutter: ^0.3.1
  equatable: ^2.1.0
  fl_chart: ^1.2.0
  flutter:
    sdk: flutter
  flutter_bloc: ^9.1.1
  flutter_local_notifications: ^22.3.0
  get_it: ^9.2.1
  go_router: ^17.5.0
  injectable: ^3.0.0
  intl: ^0.20.3
  timezone: ^0.11.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  build_runner: ^2.15.1
  injectable_generator: ^3.1.1
  drift_dev: ^2.34.5

flutter:
  uses-material-design: true
